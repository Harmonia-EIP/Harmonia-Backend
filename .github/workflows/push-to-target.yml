name: Mirror to another repo (with submodules)

on:
  push:
    branches:
      - main

env:
  TARGET_REPO_SSH: git@github.com:EpitechPromo2027/G-EIP-600-LYN-6-1-eip-swan.sallet.git
  TARGET_BRANCH: harmonia-backend

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source with submodules
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests (pytest)
        env:
          PYTHONPATH: .
          DATABASE_URL: postgresql+psycopg2://user:pass@localhost:5432/dbname
        run: |
          pytest -q tests

  mirror:
    needs: tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source with submodules
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SECRET_KEY_PUSH_MIRROR }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts

      - name: Push to target repo (with submodules)
        run: |
          git remote add mirror "${{ env.TARGET_REPO_SSH }}"
          git push mirror HEAD:${{ env.TARGET_BRANCH }} --force --recurse-submodules=on-demand
